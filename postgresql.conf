# Minimal custom config to allow external connections
# keep other settings as default unless you need to tune them

# Listen on all interfaces
listen_addresses = '*'
port = 5432

# Ensure server uses our custom pg_hba.conf
hba_file = '/etc/postgresql/pg_hba.conf'

# Optional tuning examples
# shared_buffers = 128MB
# max_connections = 100