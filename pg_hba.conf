# TYPE  DATABASE        USER            ADDRESS                 METHOD

# local connections (socket)
local   all             all                                     peer

# Allow host connections from anywhere (use a restricted CIDR in production)
# Require password authentication (md5). Replace with a restricted address range if possible.
host    all             all             0.0.0.0/0               md5
host    all             all             ::/0                    md5